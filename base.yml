---
- hosts: 127.0.0.1
  connection: local

  roles:
    - role: osx_defaults
    - role: computername
      when: computername is defined
    - role: ssh_config
    - role: sudoers
    - role: terminal
    - role: git
    - role: homebrew
    # below roles depend on homebrew
    - role: vim
    #- role: zsh

  vars:
    - username: "{{ lookup('env', 'USER') }}"

    - brew_apps:
      - tmux
      - the_silver_searcher
      - jq
      - htop-osx
      - irssi
      - markdown
      - pandoc

    - brew_cask_apps:
      - alfred
      - slate
      - rescuetime
      - flux
      - 1password
      - typinator
      - slack
      - google-chrome
      - evernote
      - omnifocus
      - dropbox
      - spotify
      - sonos
      - kaleidoscope
      - vagrant
      - virtualbox
