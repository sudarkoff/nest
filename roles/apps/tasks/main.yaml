---
- name: install brew applications
  homebrew:
    name: "{{brew_apps}}"
    state: latest

- name: install brew cask applications
  homebrew_cask:
    name: "{{item}}"
    state: present
  with_items: "{{brew_cask_apps}}"
